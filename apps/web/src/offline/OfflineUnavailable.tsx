import { EmptyState } from '../components/states'

export default function OfflineUnavailable() {
  return <EmptyState title="Conteúdo indisponível offline" description="Este conteúdo não foi preparado para uso offline. Conecte-se e atualize os dados offline." />
}
